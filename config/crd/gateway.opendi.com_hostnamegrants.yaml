---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: hostnamegrants.gateway.opendi.com
spec:
  group: gateway.opendi.com
  names:
    kind: HostnameGrant
    listKind: HostnameGrantList
    plural: hostnamegrants
    shortNames:
    - hg
    singular: hostnamegrant
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.namespace
      name: Namespace
      type: string
    - jsonPath: .spec.hostnames
      name: Hostnames
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          HostnameGrant is the Schema for the hostnamegrants API
          Records which hostnames a namespace is allowed to use (consumed by policy engine)
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HostnameGrantSpec defines the desired state of HostnameGrant
            properties:
              hostnames:
                description: Hostnames that the namespace is allowed to use
                items:
                  type: string
                minItems: 1
                type: array
              namespace:
                description: Namespace that is allowed to use these hostnames
                type: string
            required:
            - hostnames
            - namespace
            type: object
          status:
            description: HostnameGrantStatus defines the observed state of HostnameGrant
            properties:
              grantedAt:
                description: GrantedAt is the timestamp when the grant was created
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
