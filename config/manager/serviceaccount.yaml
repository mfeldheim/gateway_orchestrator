apiVersion: v1
kind: ServiceAccount
metadata:
  name: gateway-orchestrator-controller
  namespace: gateway-orchestrator-system
  labels:
    app.kubernetes.io/name: gateway-orchestrator
    app.kubernetes.io/component: manager
  annotations:
    # For IRSA - replace with your actual role ARN
    # eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/gateway-orchestrator-role
